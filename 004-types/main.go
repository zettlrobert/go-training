package main

import "fmt"

// DECLARE the VARIABLOE with the IDENTIFIER "z"
// is of TYPE string
// and ASSIGN the VALUE of "shaken, not stirred"

var z string = "Sahken, not stirred"
var a string = `Backticks make "" and returns available in String`

// this is a ATATIC programming langauge
// a VARIABLE is DECLARED to hold a VALUE of a certain TYPE

func main() {
	// Static Programming Language
	fmt.Printf(y)
	fmt.Printf("%T\n", y)

	// fmt.Printf(z)
	// fmt.Printf("%T\n", z)
}

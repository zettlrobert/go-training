package main

import (
	"fmt"
)


// Constants of a Kind Compiler gets flexibility (no type spcified)
const (
	a = 42
	b = 42.78
	c = "James Bond"
)

func main()  {
	fmt.Println(a)
	fmt.Println(b)
	fmt.Println(c)
}